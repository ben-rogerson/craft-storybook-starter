<!DOCTYPE html>
<html lang="en-US">
<head>
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta charset="utf-8" />
    <title>Welcome to the Craft Storybook Starter</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover" />
    <meta name="referrer" content="origin-when-cross-origin" />
    <style>
        html,
        body {
            font-size: 16px;
            -webkit-text-size-adjust: 100%;
            height: 100%;
            font-family: system-ui, BlinkMacSystemFont, -apple-system, 'Segoe UI', 'Roboto', 'Oxygen', 'Ubuntu', 'Cantarell', 'Fira Sans', 'Droid Sans', 'Helvetica Neue', sans-serif;
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            padding: 0;
            background-color: hsl(212, 10%, 93%);
            color: hsl(212, 20%, 20%);
            display: flex;
            line-height: 1.5;
        }

        h1 {
            margin-top: 0;
        }

        p {
            margin-bottom: 1.4em;
        }

        a {
            color: #0d78f2;
            text-decoration: none;
        }

        code {
            background: #EEE;
            border-radius: 2px;
            padding: 0 2px;
        }

        a:hover {
            text-decoration: underline;
        }

        #container {
            flex-grow: 1;
            width: 100%;
        }

        #modal {
            background: #fff;
        }

        .content {
            padding: 2em;
        }

        .content > *:last-child {
            margin-bottom: 0;
        }

        .example {
            background: #EEE;
            padding: 2em;
            margin-top: 2em;
            margin-bottom: 2em;
            display: block;
        }

        @media (min-width:480px) {
            body {
                background-image: url("{{ view.getAssetManager().getPublishedUrl('@app/web/assets/installer/dist', true, 'images/install-bg-1920.png') }}");
                background-repeat: no-repeat;
                background-size: cover;
                background-position: center center;
            }

            #container {
                display: flex;
                align-items: normal;
                justify-content: normal;
            }

            .content {
                padding: 4em;
            }
        }

        @media (min-width:480px) and (min-height: 376px) {
            #container {
                padding: 2em;
                align-items: center;
                justify-content: center;
            }

            #modal {
                border-radius: 4px;
                overflow: auto;
                box-shadow: 0 25px 100px rgba(0, 0, 0, 0.5);
            }
        }

        @media only screen and (min-width: 1921px) {
            body {
                background-image: url("{{ view.getAssetManager().getPublishedUrl('@app/web/assets/installer/dist', true, 'images/install-bg-5120.png') }}");
            }
        }

        @media only screen and (-webkit-min-device-pixel-ratio: 1.5) and (min-width: 961px),
        only screen and (-moz-min-device-pixel-ratio: 1.5) and (min-width: 961px),
        only screen and (-o-min-device-pixel-ratio: 3/2) and (min-width: 961px),
        only screen and (min-device-pixel-ratio: 1.5) and (min-width: 961px),
        only screen and (min-resolution: 1.5dppx) and (min-width: 961px) {
            body {
                background-image: url("{{ view.getAssetManager().getPublishedUrl('@app/web/assets/installer/dist', true, 'images/install-bg-5120.png') }}");
            }
        }
    </style>
</head>
<body class="ltr">
<div id="container">
    <div id="modal">
        <div id="main">
            <div class="content">
                <h1>Hey there!</h1>
                <p>This is the component:<br/><code>templates/components/input.twig</code></p>

                <div class="example">
                    {% include "components/input" with {
                        label: "Enter your greeting",
                        value: "Welcome to Craft CMS",
                        error: "",
                        isDisabled: false,
                        isRequired: false,
                        placeholder: "Eg: Go get crafty!",
                    } %}
                </div>

                <p>Run <code>npm run storybook</code> to see the<br/> component in storybook.</p>
            </div>
        </div>
    </div>
</div>
</body>
</html>
